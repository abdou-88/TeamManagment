import * as React from "react";
import styles from "../../styles/ScoreCard.module.scss";

interface FooterProps {
  
}

const Footer: React.FC<FooterProps> = ({}) => (
  
    <div className={styles.ScoreCFooter}></div>
  
);

export default Footer;
